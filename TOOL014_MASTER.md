# TOOL014 MASTER — 14번 홈페이지 수정 도구

상태: ACTIVE / TOOL-SPECIFIC SINGLE SOURCE
현재 배포상태: BLOCKED_EXTERNAL(PAGES_NOT_ENABLED)
기준일: 2026-08-30

이 문서는 14번 도구 고유 규칙만 보관한다. WIC 공통 운영규칙은 `obk369369-spec/20-operational-manual-viewer/WIC_GLOBAL_OPERATING_RULES.md`를 우선하며 공통 규칙을 중복 복제하지 않는다.

## 1. 목적
- 기존 월드산업정보센터 공식 홈페이지를 안전하게 분석하고, 오래된 구조·메뉴·콘텐츠·사용성 문제를 찾아 수정 후보를 제시하고 검증된 변경만 반영하는 도구다.
- 공식 운영사이트를 실험판으로 직접 건드리지 않고, 미리보기/스테이징에서 변경 결과를 확인한 뒤 실제 반영하는 구조를 우선한다.

## 2. 공식 홈페이지 보호
- 라이브 홈페이지를 바로 덮어쓰지 않는다.
- 수정 전 현재 구조/메뉴/페이지/링크를 확인하고 변경 대상만 좁게 다룬다.
- 정상 동작 중인 주문·문의·회원·자료 링크 등 기존 기능을 새 디자인 때문에 깨지 않는다.
- 서버 전체 복제나 불필요한 신규 호스팅을 기본 전제로 두지 않는다.
- 되돌리기 가능한 변경 단위를 유지한다.

## 3. 수정 후보 판단
- 오래된 메뉴/카테고리, 중복 콘텐츠, 모바일 가독성, 링크 오류, 불필요한 화면, 고객이 자료를 찾기 어려운 동선 등을 후보로 본다.
- 단순히 최신 디자인처럼 보이게 만드는 것이 목적이 아니다. 고객이 자료를 찾고 문의/구매까지 가는 실제 사용성을 우선한다.
- 기존 기능을 없애기 전 현재 사용 여부와 연결관계를 확인한다.
- 자동 판단 근거가 부족한 항목은 삭제/변경을 강행하지 않고 HOLD/제안으로 둔다.

## 4. 미리보기/비교
- 변경 전/후 차이를 사용자가 한눈에 볼 수 있게 한다.
- 수정 제안은 가능하면 대상 위치, 현재 상태, 제안 상태, 이유를 연결한다.
- 대규모 전체 개편보다 실제 문제가 확인된 부분부터 점진적으로 수정한다.
- 12번 서브사이트에서 검증된 화면/동선이 실제 재사용 가치가 있을 때만 좁게 가져온다.

## 5. 비용/배포
- 기존 호스팅·GitHub·정적 미리보기 등 비용이 추가되지 않는 구조를 우선한다.
- 새 서버/도메인/유료 서비스는 실제 필요성이 확인되기 전 기본 요구사항으로 두지 않는다.
- 배포 권한/호스팅 설정 문제는 코드 문제와 분리해 BLOCKED_EXTERNAL/HOLD로 표시한다.

## 6. 실제 검증
- 코드/화면 파일이 저장소에 있는 것만으로 홈페이지 수정이 완료된 것이 아니다.
- 미리보기 또는 공개 테스트 URL에서 변경 대상 화면과 링크가 실제로 동작해야 한다.
- 새 변경 범위 FIRST_VALIDATION 1회만 수행하고 기존 정상 영역은 SKIP_REUSE한다.
- 현재 canonical은 `public/index.html`이며, 좁은 코드 확인에서 명백한 미구현/문법 오류는 확인되지 않은 상태다.
- 현재 저장소의 GitHub Pages가 비활성화되어 공개 런타임 검증이 막혀 있으므로 `BLOCKED_EXTERNAL(PAGES_NOT_ENABLED)`로 유지한다.
- 재개조건: 기존 저장소의 Pages 활성화 후 변경 범위 FIRST_VALIDATION 1회.

## 7. 완료 판정
- 과거 대화의 가상 진행률·남은시간·백그라운드 개발 주장은 현재 완료 증거로 승계하지 않는다.
- 실제 수정파일, 실제 테스트, 배포/미리보기 결과를 기준으로 판단한다.
- `코드 준비 완료`, `수정 예정`, `테스트 계속 중`만으로 COMPLETE 처리하지 않는다.

## 8. 2026-08-30 대화기록 catch-up
- 제공된 14번 과거 기록에서 공식 홈페이지 보호, 안전한 단계별 수정, 실제 사용성, 비용 최소화, 검증 후 반영 원칙을 현재 상태와 통합했다.
- 증거 없는 장시간 진행률·ETA·백그라운드 작업 보고는 현재 규칙으로 복원하지 않는다.
- 이후 14번 대화창에서 사용자가 `업데이트`라고 입력하면 신규 영구 피드백만 이 MASTER와 대조해 DIFF ONLY 반영한다.
- 동일 규칙은 SKIP_REUSE하고 같은 범위 충돌은 더 최신의 명시적 사용자 지시를 우선한다.
- 실제 GitHub write/commit/read-back 없이 업데이트 완료라고 보고하지 않는다.

## 9. 안전 변경 패키지 계약 (2026-08-30)
- HTML 생성 전 기준 revision/SHA와 변경 이유를 필수로 저장한다.
- 생성물에는 `WIC_SAFE_CHANGE` manifest를 포함하고 `autoDeploy=false`를 고정한다.
- CTA 링크는 `#내부링크`, HTTPS, `mailto:`만 허용한다. 불명확하거나 실행 가능한 임의 scheme은 HOLD한다.
- 현재 도구는 안전한 변경 HTML 패키지를 생성하지만 실제 공식 홈페이지에 자동 반영하지 않는다.
- 실제 운영 반영은 원본 revision 재대조, 별도 staging 확인, 명시적 배포 권한이 있어야 한다.
